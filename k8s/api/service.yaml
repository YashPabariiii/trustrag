apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: trustrag
  labels:
    app: trustrag
    component: api
spec:
  type: ClusterIP
  selector:
    app: trustrag
    component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
